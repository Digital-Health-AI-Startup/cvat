# Nuclio Deployment

Forward port 8070 to the nuclio dashboard by running:
`kubectl port-forward --namespace cvat $(kubectl get pods --namespace cvat -l nuclio.io/app=dashboard -o jsonpath='{.items[0].metadata.name}') 8070:8070`

View Nuclio Dashboard by browsing to:
`http://localhost:8070`

Create project named cvat:
`nuctl create -n cvat project cvat`

Deploy the segment anything model by running the following command from the root directory of our cvat github repo:
`nuctl deploy --namespace cvat --project-name cvat --path serverless/pytorch/facebookresearch/sam/nuclio`